{
  "name": "terminal-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ xtokenmarket/terminal-subgraph",
    "create-local": "graph create --node http://localhost:8020/ xtokenmarket/terminal-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ xtokenmarket/terminal-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 xtokenmarket/terminal-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
